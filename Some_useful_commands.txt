git status
git init
touch .gitignore
git log
git diff
pwd
git add .
git commit -m ""
mv "file you want to change its name" "what you want to set its name"
rm "file name"
git checkout -- file name
git checkout -f (this used to backup all the files again from the last commit)
